{
  "routes": [
    {
      "method": "GET",
      "path": "/package-images",
      "handler": "package-images.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/package-images/count",
      "handler": "package-images.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/package-images/:id",
      "handler": "package-images.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/package-images",
      "handler": "package-images.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/package-images/:id",
      "handler": "package-images.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/package-images/:id",
      "handler": "package-images.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
