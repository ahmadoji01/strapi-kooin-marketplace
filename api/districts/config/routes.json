{
  "routes": [
    {
      "method": "GET",
      "path": "/districts",
      "handler": "districts.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/districts/count",
      "handler": "districts.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/districts/:id",
      "handler": "districts.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/districts",
      "handler": "districts.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/districts/:id",
      "handler": "districts.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/districts/:id",
      "handler": "districts.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
